// Copy this file to config.js and fill in your Supabase project values.
// IMPORTANT: config.js should be committed for GitHub Pages (it contains ONLY the anon key).

window.CREATORBOOK_CONFIG = {
  // Project Settings -> API
  supabaseUrl: "https://YOUR_PROJECT_REF.supabase.co",

  // Project Settings -> API Keys -> Publishable (anon) key
  // Safe to expose in browser when RLS policies are on.
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY",
};
